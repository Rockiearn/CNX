services:
  app:
    build: .
    container_name: CNX
    volumes:
      - .:/app
    ports:
      - "8888:8888"   # Jupyter
    command: jupyter notebook --ip=0.0.0.0 --port=8888 --no-browser --allow-root --NotebookApp.token=''
    stdin_open: true
    tty: true