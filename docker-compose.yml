services:
  app:
    build: .
    container_name: CNX
    volumes:
      - .:/app
    ports:
      - "8888:8888"   # Jupyter
    environment:
      - PYTHONPATH=/app:/app/src     
    working_dir: /app  
    stdin_open: true
    tty: true